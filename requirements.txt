# Core dependencies
python-dotenv>=1.0.0

# API clients
openai>=1.0.0
pinecone>=5.0.0

# LangChain for conversation memory
langchain>=0.1.0
langchain-openai>=0.0.5

# UI
streamlit>=1.28.0

# Testing (not needed in production but included for CI)
pytest>=7.0.0
